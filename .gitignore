node_modules/
/dist/
*.local

# Go
backend/vendor/

# Go build output (e.g. `go build` in backend/ produces backend/backend or server)
/backend/backend
/backend/server

# Tilt dev — local env override (keep dev/.env.example tracked)
dev/.env

# OS
.DS_Store
